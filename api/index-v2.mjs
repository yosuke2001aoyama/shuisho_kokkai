import{VERSION,clean}from'./lib/core.mjs';
import{build,searchAll,selfTest}from'./lib/draft.mjs';
import{writtenIndexStats}from'./lib/written.mjs';
const json=(res,status,body)=>{res.statusCode=status;res.setHeader('Content-Type','application/json; charset=utf-8');res.setHeader('Cache-Control','no-store, max-age=0');res.end(JSON.stringify(body))};
export default async function handler(req,res){try{const u=new URL(req.url,'https://local');if(u.pathname.endsWith('/health'))return json(res,200,{ok:true,version:VERSION});if(u.pathname.endsWith('/self-test'))return json(res,200,selfTest());if(u.pathname.endsWith('/smoke-test')){const speech=await build('speech','我が国はアメリカの言いなりなのか。','prime'),written=await build('written','尖閣諸島、竹島及び北方領土はいずれも我が国固有の領土か。','minister'),precedent=await build('written','暗号資産に対する基本的な認識を問う。','minister'),guard=await build('speech','岸田外交の基本姿勢について、政府の認識を問う。','minister'),indexStats=await writtenIndexStats(),checks={speechIntent:speech.draft.includes('主体的')&&!speech.draft.includes('沖縄の未来'),writtenNoRespondent:written.respondent===null,writtenMultiIssue:written.issueCount===3,plainStyle:!/(まいります|ございます|しております)/.test(speech.draft+written.draft+precedent.draft+guard.draft),writtenIndex:indexStats.count>0,writtenSource:precedent.references.some(x=>x.sourceType==='written'),topicGuard:!/(国家公務員|労働基本権|人事院勧告)/.test(guard.draft),alwaysDraft:Boolean(speech.draft&&written.draft&&precedent.draft&&guard.draft)};return json(res,200,{version:VERSION,passed:Object.values(checks).every(Boolean),checks,indexStats,samples:{speech,written,precedent,guard}})}if(u.pathname.endsWith('/search')){const q=clean(u.searchParams.get('q'));if(!q)return json(res,400,{error:'検索語を入力してください。'});const respondent=['prime','minister','official'].includes(u.searchParams.get('respondent'))?u.searchParams.get('respondent'):'minister',results=await searchAll(q,respondent);return json(res,200,{query:q,version:VERSION,coverage:{note:'国会答弁、衆参両院の質問主意書答弁書、会見・演説、インタビュー・寄稿及び政府公式資料を横断し、質問意図との適合性で絞り込んでいる。'},results})}if(u.pathname.endsWith('/draft')){if(req.method!=='POST')return json(res,405,{error:'Method not allowed'});let raw='';for await(const c of req)raw+=c;const body=raw?JSON.parse(raw):{},question=clean(body.question),mode=body.mode==='written'?'written':'speech',respondent=['prime','minister','official'].includes(body.respondent)?body.respondent:'minister';if(question.length<8)return json(res,400,{error:'質問を8文字以上で入力してください。'});return json(res,200,{...(await build(mode,question,respondent)),mode,generatedBy:`draft-engine-v${VERSION}`,disclaimer:'起案補助用。正式使用前に主管府省で最新の事実関係及び政府方針を確認すること。'})}return json(res,404,{error:'Not found'})}catch(e){return json(res,502,{error:e?.message||'処理に失敗した。',version:VERSION})}}
