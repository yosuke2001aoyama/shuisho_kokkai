import{VERSION,toPlainStyle,categoryOfSpeech,bestPassage,relevance}from'./core.mjs';
export async function dietSearch(issue,respondent){const all=[];for(const term of issue.queries.slice(0,5)){try{const p=new URLSearchParams({maximumRecords:'80',recordPacking:'json',any:term}),r=await fetch(`https://kokkai.ndl.go.jp/api/speech?${p}`,{headers:{Accept:'application/json','User-Agent':`ShuishoKokkai/${VERSION}`},signal:AbortSignal.timeout(7000)});if(r.ok)all.push(...((await r.json()).speechRecord||[]))}catch{}}const seen=new Set(),out=[];for(const x of all){const id=x.speechID||x.speechURL;if(!id||seen.has(id))continue;seen.add(id);const category=categoryOfSpeech(x);if(!category)continue;const title=`${x.nameOfHouse||''} ${x.nameOfMeeting||''}`.trim(),phrase=bestPassage(x.speech,issue,title);if(!phrase)continue;const rawScore=relevance(phrase,issue,title);if(rawScore<45)continue;out.push({id,sourceType:'answer',category,phrase:toPlainStyle(phrase),title,url:x.speechURL||x.meetingURL||'https://kokkai.ndl.go.jp/',sourceName:'国会会議録検索システム',date:x.date||'',speaker:x.speaker||'',speakerPosition:x.speakerPosition||'',score:rawScore+(category===respondent?90:0)})}return out.sort((a,b)=>b.score-a.score).slice(0,14)}
